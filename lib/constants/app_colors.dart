import 'package:flutter/material.dart';

const background = Color.fromRGBO(36, 36, 36, 1);
const inputField = Colors.white;

// purple
const deepPurple = Color.fromRGBO(91, 12, 104, 1);
const midPurple = Color.fromRGBO(152, 64, 167, 1);
const lightPurple = Color.fromRGBO(241, 156, 255, 1);

// Animation color
const mainLoadingAnimationColor = Colors.white;
